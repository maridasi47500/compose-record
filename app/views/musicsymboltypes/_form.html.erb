<%= form_with(model: musicsymboltype) do |form| %>
  <% if musicsymboltype.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musicsymboltype.errors.count, "error") %> prohibited this musicsymboltype from being saved:</h2>

      <ul>
        <% musicsymboltype.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
