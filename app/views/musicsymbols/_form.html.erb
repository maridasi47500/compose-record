<%= form_with(model: musicsymbol) do |form| %>
  <% if musicsymbol.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musicsymbol.errors.count, "error") %> prohibited this musicsymbol from being saved:</h2>

      <ul>
        <% musicsymbol.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :musicsymboltype_id, style: "display: block" %>
    <%= form.number_field :musicsymboltype_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
